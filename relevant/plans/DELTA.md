# DELTA — Build session 2026-07-18 (Claude Fable 5)

Deviations from plans 01–05, per the amend-and-document rule in the FABLE brief.

## App package location
The reorganized repo keeps the site package at **`nikah-web/`** (package name `nikah-web`);
the deleted `nikah-web/` tree's source (app/, components/, lib/, scripts/) was restored
from git HEAD into `nikah-web/` and adapted. `copy-assets` now mirrors `nikah-web/assets/` →
`nikah-web/public/assets/`.

## L0 envelope → gate-first (plan 01 Q2, plan 03 §4)
Collapsed the CSS envelope into the Gate as the single unlock ritual. Two taps before
the hero (envelope + Buka Undangan) doubled the ceremony without adding meaning; the
Gate already carries the guest name, monogram wreath, and floral border. Audio + iOS
gyro permission unlock inside the one "Buka Undangan" gesture.

## Hero composition: sky extension instead of cover-crop (plan 03 §4 L3)
On tall phones, cover-cropping the 4:5 loop put the names over the couple's faces
(GG-Hero violation). The hero now anchors the **full uncropped artwork** to the bottom
of the viewport and extends the painted sky upward. Text owns the extended sky; no
transform ever touches the video.

**Update (user-requested, 2nd sanctioned gpt-image-2 run):** the flat gradient
extension is now a **painted sky band** (`assets/scenes/hero-sky-band.webp`, 12 KB,
generated by `scripts/generate-sky-band.mjs`). The model painted the continuation
directly above the real poster on one canvas — wispy ivory clouds, two distant doves,
edge-drifting sakura petals, calm pale center for the names — then the band was cropped
back out. On-page, its lower quarter overlaps the video's own sky and mask-fades into
it (`mask-image` 58%→100%), so the two skies cross-dissolve with no visible junction.
The flat gradient remains beneath as the pre-load wash. Review copy:
`relevant/01-hero-scenes-video/hero-sky-band.webp`.

## Music icons → inline SVG (plan 02 P1 #6)
The generated `music-note-icon(-muted).webp` pair is opaque and unmatched. The
persistent toggle uses a tiny inline SVG note (sanctioned fallback in plan 05 §7).
The icon keepers were **not promoted**.

## Ivory-plate knockouts (plan 02 soft issues)
`welcome-dove-floral` and `gate-monogram-frame` shipped with their flat ivory plates
chroma-keyed to alpha (scripts/knockout-ivory.mjs) so they sit directly on the paper.
The same key destroyed the white cat and envelope art (paint ≈ plate), so
`loading-sleeping-cat` and `gift-envelope-icon` ship opaque with soft radial CSS masks.

## Japan Dream ships textless (plan 04 §6)
`03-copywriting.md` has no Japan-section copy and copy may not be invented. The sakura
campus scene ships as a pure full-bleed visual breath (HIGH tier: three drifting CSS
petals) between the story and the practical act.

## Closing copy split (plan 04 §9)
The closing scene's sky is too short for all four locked lines on mobile — the
emphasis overlapped faces. The two short italic lines stay in the sky band; the
emphasis + names + hashtag moved to the paper directly below the scene.

## ch04 — one gpt-image-2 edit run (plan 02 §5, GG-Story)
Single sanctioned `openai/gpt-image-2/edit` run (scripts/edit-ch04.mjs) referencing the
existing ch04 + hero-main. Result `relevant/04-story/story-ch04-ldr-tokyo-v2.webp`
adds Tokyo Tower + sakura branch + dusk window while keeping the heart connector and
identities. **Promoted to shipping (158 KB); human sign-off still requested.** The
original keeper is untouched; rollback = re-run remediate-assets.mjs.

## Story chapter framing
Chapters render as square polaroids (~85vw, alternating ±1°) with title + body below
the frame; scrapbook strip (4 harmonized photos, washi tape, lightbox) placed after
ch06 rather than between ch03–05 — keeps the illustrated arc unbroken.

## Desktop treatment
Above ~560px the experience lives in a centered 480px paper column on a cream field
with two quiet floral corners. Keeps GG-Hero composition intact at any viewport.

## Fonts
Body sans switched Inter → Plus Jakarta Sans per the FABLE brief's brand-voice rule
(overrides the older AGENTS.md note that named Inter).
