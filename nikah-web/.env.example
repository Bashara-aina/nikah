# Public site origin — used in metadata + OG tags.
NEXT_PUBLIC_SITE_URL=https://nikah.example

# Google Apps Script Web App URL for RSVP submission (server-side only).
APPS_SCRIPT_URL=

# fal.ai key — consumed only by scripts/generate-assets.mjs, never bundled to the site.
FAL_KEY=